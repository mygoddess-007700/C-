/*
时间：2020年10月4日18:25:45
目的：对且，或的运用与对真假的理解 

*/

# include <stdio.h>
int main(void)
{
	int i = 10;
	int k = 20;
	int m;
	
	m=(3>2) && (k=8);  //不含有分号的是表达式 含有分号的是语句（若计算机已经判断出且语句为假，那么将不再执行后续操作，例如：m = (1>2） && (k=5);则 m = 0， k = 20 
	printf("m = %d, k = %d\n", m, k);
	
	return 0;
}

/*输出结果：
m = 1, k = 8

--------------------------------
*/

 
